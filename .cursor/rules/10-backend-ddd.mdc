---
description: DDD-структура бэкенда (Go) и правила границ агрегатов
globs: backend/**/*.go
alwaysApply: false
---

# Целевая структура

```
backend/internal/
  domain/{user,project,task,report}/   # агрегаты, VO, интерфейсы репозиториев
  application/                         # cross-aggregate оркестраторы
  infrastructure/repository/           # GORM-реализации репозиториев
  interface/http/ (=handlers)          # тонкий HTTP-слой
```

Источник истины — [docs/architecture/aggregates.md](docs/architecture/aggregates.md).

# Границы слоёв

- `domain/*` **не импортирует** `gorm`, `gin`, `net/http`, `database/sql`. Только чистый Go + интерфейсы репозиториев, объявленные здесь же.
- `application/*` — координирует несколько агрегатов в одной транзакции. Имена файлов: `project_deletion_service.go`, `member_removal_service.go`, `task_move_service.go`, `task_assign_service.go`.
- `infrastructure/repository/*` — единственное место с GORM и SQL.
- `handlers/*` — тонкий слой: парсинг body, вызов сервиса, `handleServiceError(c, err)`, формирование ответа. Никакой бизнес-логики, никаких прямых запросов в БД.

# Инварианты и транзакции

- **Одна транзакция = один агрегат.** Исключения — только через сервис в `application/`, явно прокомментированные.
- Cross-aggregate операции в этом проекте: удаление проекта с каскадом, удаление участника с переносом задач, перенос задачи между секциями/проектами, назначение исполнителя с проверкой членства. См. таблицу границ в [docs/architecture/aggregates.md](docs/architecture/aggregates.md).
- Не трогаем несколько корней в одной функции доменного слоя — только через `application/`.

# Ошибки

- Доменные ошибки — типизированные переменные в том же пакете:

```go
var ErrProjectNotFound = errors.New("project not found")
```

- HTTP-хэндлеры НЕ формируют текст ошибки вручную, а пробрасывают через [backend/internal/handlers/errors.go](backend/internal/handlers/errors.go) / `handleServiceError`.

# Переезд из текущего кода

Миграция инкрементальная. Пока новый пакет не готов — старый код в [backend/internal/services/](backend/internal/services/) и [backend/internal/models/](backend/internal/models/) остаётся и работает. Каждый PR:

1. Компилируется (`go build ./...`).
2. Проходит `make backend-test`.
3. Двигает один слой/агрегат.
